# Vite docs app (./docs)
docs/node_modules
docs/dist

# Monorepo
node_modules
.pnpm-store
.turbo
dist
# Next build output (deploy ships apps/web/prebuilt-next.tar.gz instead)
**/.next
*.tsbuildinfo

# Go (prebuilt-api.tar.gz is committed; extracted linux-amd64/ stays local)
apps/api/prebuilt/
apps/api/bin/
*.exe
*.test

# Env
.env
.env.local
.env.*.local

# General
.DS_Store
*.local

# Playwright
playwright-report/
test-results/
apps/web/e2e/.auth/
