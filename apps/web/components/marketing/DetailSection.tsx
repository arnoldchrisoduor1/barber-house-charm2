export function DetailSection() {
  return (
    <section className="border-y border-border/60 bg-muted/10 py-24">
      <div className="mx-auto max-w-6xl px-4 sm:px-6">
        <p className="label-eyebrow">Built for operators</p>
        <h2 className="mt-3 max-w-2xl font-display text-4xl">Mode-aware language, nav, and theming — out of the box.</h2>
        <p className="mt-4 max-w-2xl text-muted-foreground">
          Each Haus speaks your trade: barber scorecards, spa packages, clinic consent, mobile coverage zones, and
          retail inventory — all from one registry, resolved server-side.
        </p>
      </div>
    </section>
  );
}
